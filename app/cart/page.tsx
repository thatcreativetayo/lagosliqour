import CartClient from "./CartClient";

export const metadata = {
  title: "Cart | Lagos Liquor",
  description: "Review your cart of premium wines and spirits.",
};

export default function CartPage() {
  return <CartClient />;
}
